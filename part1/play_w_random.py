# Importing the random library to generate random numbers
import random

# Using random.sample() to generate 10 unique random numbers
numbers = random.sample(range(1, 11), 10)

print("Random numbers:", numbers)
